commands:
  01_install_redis:
    command: "yum install -y redis || (amazon-linux-extras install epel -y && yum install -y redis)"
  02_start_redis:
    command: "systemctl enable redis && systemctl start redis"
